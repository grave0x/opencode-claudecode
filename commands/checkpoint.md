# /checkpoint

Create a manual session snapshot. Captures file state and conversation context.

Usage:
- `/checkpoint` — create checkpoint with auto-label
- `/checkpoint "before auth refactor"` — create with label

Checkpoints stored in `~/.opencode-sessions/<session-id>/checkpoints/`
