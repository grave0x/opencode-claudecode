# /yolo

Toggle auto-approve mode through permission levels.

Cycle: manual → acceptEdits → plan → auto → yolo → manual

Usage:
- `/yolo` — advance to next permission mode
- `/permissions manual` — set specific mode
- `/permissions` — show current mode

See AGENTS.md auto-approve-rules for mode descriptions.
