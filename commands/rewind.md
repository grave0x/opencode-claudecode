# /rewind

Undo session to a previous checkpoint. Restores file state and rolls back conversation.

Usage:
- `/rewind` — undo to last checkpoint
- `/rewind 3` — undo to checkpoint #3
- `/rewind --soft` — rollback conversation only, keep files
