# /effort

Set reasoning effort level for current model.

Usage:
- `/effort low` — minimal reasoning, fast responses
- `/effort medium` — balanced (default)
- `/effort high` — maximum reasoning depth
- `/effort` — show current effort level
