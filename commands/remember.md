# /remember

Save a fact to cross-session memory. Auto-tagged with current working directory.

Usage:
- `/remember "the build command is \`npm run build\`"`

Stored in ~/.config/opencode/memory/ as Markdown files with cwd tag, category, and timestamp.
