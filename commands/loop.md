# /loop

Recurring instruction loop. Repeats a prompt on each turn until stopped.

Usage:
- `/loop "fix any lint errors in diffs"` — set recurring prompt
- `/loop clear` — clear active loop
- `/loop show` — show active loop
