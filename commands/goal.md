# /goal

Autonomous mode. Agent works independently toward goal without per-turn approval.

Usage:
- `/goal "refactor auth to use JWT"` — start goal mode
- `/goal status` — show progress
- `/goal pause` — pause execution
- `/goal resume` — continue
- `/goal stop` — terminate goal mode
