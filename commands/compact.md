# /compact

Compress session history to reduce token usage. Summarizes older turns into condensed form while preserving key decisions, file changes, and state.

Usage:
- `/compact` — compress history from beginning
- `/compact --keep 10` — keep last N turns uncompressed
