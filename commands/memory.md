# /memory

Search and manage cross-session memory.

Usage:
- `/memory search <query>` — search stored memories
- `/memory list` — list all memories
- `/memory list --tag "project-x"` — filter by tag
- `/memory clear` — wipe all memory
- `/memory clear --id 3` — delete specific memory entry
