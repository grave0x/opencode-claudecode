## Objective
- Add system-prompt marketplace: browse & download from 116 leaked prompts (33 companies) hosted on YeeKal/leaked-system-prompts
- Fix existing registry `source_url` entries: all now point to raw.githubusercontent.com (raw text) instead of leaked-system-prompts.com (HTML)

## Important Details
- YeeKal/leaked-system-prompts has 116 prompts across 33 companies — static `market-index.json` built via `gh api`, committed to git as fallback
- `source_url` in `registry.json` now uses raw.githubusercontent.com URLs for all 12 entries — `fetch` command downloads raw text, not HTML
- Raw downloads verified: downloaded file `raw/anthropic-claude-design.txt` is 73KB real prompt text with YAML frontmatter
- Duplicate IDs in market index: e.g., `anthropic-claude-3-sonnet` appears twice (different dates, same generated `id`). `list`/`search` shows duplicates; `info`/`download` uses first match

## Work State
### Completed
- Built static `market-index.json` from YeeKal repo: 116 prompts × 33 companies
- `market list` — groups by company, shows download status (✓/○), size, date
- `market search <query>` — filters across name/company/tags
- `market info <id>` — shows company, date, size, tags, download status
- `market download <id>` — fetches raw text from raw.githubusercontent.com, saves to `raw/<id>.txt`, registers in `registry.json`
- Fixed all 12 registry entries: `source_url` → raw.githubusercontent.com (was leaked-system-prompts.com HTML)
- Verified downloaded file contains real prompt text (not HTML)
- Plugin tools: `system.prompt.market.{list,search,info,download,cache}` registered

### Active
- (none)

### Blocked
- (none)

## Next Move
1. Fix duplicate ID collision in market index — prompts with same name but different dates get same `id`. Either deduplicate or append date suffix
2. Add `market refresh` command to update `market-index.json` from GitHub API on demand
3. Sync existing raw text files — re-fetch stale entries from new raw.githubusercontent.com URLs to ensure content is raw text (not HTML from old source)

## Relevant Files
- `system-prompts/market-index.json` — static market index, 116 prompts, committed to repo
- `tools/system-prompts.js` — CLI with market subcommands (list/search/info/download/cache)
- `plugins/system-prompts/plugin.js` — plugin with 5 market tool registrations
- `system-prompts/registry.json` — local prompt registry, all source_urls now point to raw text
- `system-prompts/raw/` — downloaded raw prompt text files
